import styled from "styled-components";

export const MessageDisplayHeaderContainer = styled.header`
  position: relative;
  font-size: 1.2rem;
  font-weight: 600;
  padding: 10px;

  svg {
    position: absolute;
    right: 3px;
    top: 3px;
  }
`